// create the controller and inject Angular's $scope
jchat.controller('mainController', function($scope, $http,$routeParams) {
    // var city = null;
    // if(!$routeParams.hasOwnProperty('city') || $routeParams.city === '' || $routeParams.city === "undefined" || $routeParams.city === null) {
    //     postData = '{"city": "san%20francisco"}';
    // }  else {
    //     postData = '{"city": "'+$routeParams.city+'"}';
    // }
    // var url = 'http://localhost:3000/weather';

    // Save for later to do just get access
    // $http.get(url).success(function(data) {
    //     $scope.weather_data = data;
    // });

    // $http.post(url, postData).success(function(data) {
    //     $scope.weather_data = data;
    // });
});